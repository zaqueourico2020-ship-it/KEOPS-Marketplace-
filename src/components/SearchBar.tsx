export default function SearchBar() {
  return (
    <input
      type="text"
      placeholder="Pesquisar produtos..."
      style={{
        width: "100%",
        padding: "12px",
        borderRadius: "12px",
        border: "1px solid #ccc",
        margin: "16px 0",
      }}
    />
  );
}
