export default function Header() {
  return (
    <header
      style={{
        background: "#6D28D9",
        color: "white",
        padding: "16px",
        display: "flex",
        justifyContent: "space-between",
        alignItems: "center",
      }}
    >
      <h1>KEOPS</h1>
      <span>🛒</span>
    </header>
  );
}
